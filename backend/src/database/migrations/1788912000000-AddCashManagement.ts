import { MigrationInterface, QueryRunner } from 'typeorm';
export class AddCashManagement1788912000000 implements MigrationInterface { async up(q: QueryRunner): Promise<void> { await q.query(`CREATE TABLE "cash_settings" ("id" integer PRIMARY KEY, "feePerPlayer" integer NOT NULL DEFAULT 15000)`); await q.query(`INSERT INTO "cash_settings" ("id", "feePerPlayer") VALUES (1, 15000)`); await q.query(`CREATE TABLE "cash_expenses" ("id" SERIAL PRIMARY KEY, "reason" character varying NOT NULL, "quantity" integer NOT NULL, "unitPrice" integer NOT NULL, "amount" integer NOT NULL, "createdAt" TIMESTAMP NOT NULL DEFAULT now())`); } async down(q: QueryRunner): Promise<void> { await q.query(`DROP TABLE "cash_expenses"`); await q.query(`DROP TABLE "cash_settings"`); } }
