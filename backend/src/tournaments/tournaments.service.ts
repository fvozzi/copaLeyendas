import { BadRequestException, Injectable, NotFoundException } from '@nestjs/common'; import { InjectRepository } from '@nestjs/typeorm'; import { Repository } from 'typeorm'; import { Court } from '../courts/court.entity'; import { PairRegistration } from '../registrations/pair-registration.entity'; import { Tournament } from './tournament.entity'; import { TournamentCategory } from './tournament-category.entity'; import { Zone } from './zone.entity'; import { ZoneEntry } from './zone-entry.entity'; import { TournamentMatch } from './tournament-match.entity'; import { MatchStatus, ParticipantSource } from './tournament.enums';
@Injectable() export class TournamentsService { constructor(@InjectRepository(Tournament) private t: Repository<Tournament>, @InjectRepository(TournamentCategory) private tc: Repository<TournamentCategory>, @InjectRepository(Zone) private z: Repository<Zone>, @InjectRepository(ZoneEntry) private e: Repository<ZoneEntry>, @InjectRepository(TournamentMatch) private m: Repository<TournamentMatch>, @InjectRepository(Court) private c: Repository<Court>, @InjectRepository(PairRegistration) private r: Repository<PairRegistration>) {} list() { return this.t.find({ order: { startsAt: 'DESC', id: 'DESC' } }); } create(dto: Partial<Tournament>) { return this.t.save(this.t.create(dto)); } async addCategory(tournamentId: number, dto: Partial<TournamentCategory>) { return this.tc.save(this.tc.create({ ...dto, tournamentId, pointsPerSet: dto.pointsPerSet ?? 25, setsToWin: dto.setsToWin ?? 1, zoneSize: dto.zoneSize ?? 4, registrationsOpen: dto.registrationsOpen ?? true })); } async createZone(dto: { tournamentCategoryId: number; courtId: number; name: string; capacity: number }) { if (!(await this.c.findOneBy({ id: dto.courtId }))) throw new NotFoundException('Cancha no encontrada'); return this.z.save(this.z.create(dto)); } async addEntry(zoneId: number, registrationId: number) { const zone = await this.z.findOneBy({ id: zoneId }); if (!zone) throw new NotFoundException('Zona no encontrada'); if (await this.e.countBy({ zoneId }) >= zone.capacity) throw new BadRequestException('La zona alcanzo su cupo'); return this.e.save(this.e.create({ zoneId, registrationId })); } async fixture(zoneId: number) { const entries = await this.e.find({ where: { zoneId }, order: { seed: 'ASC', id: 'ASC' } }); if (![3, 4].includes(entries.length)) throw new BadRequestException('La zona debe tener 3 o 4 parejas'); await this.m.delete({ zoneId }); const direct = (a: number, b: number, order: number) => this.m.save(this.m.create({ zoneId, matchOrder: order, homeRegistrationId: a, awayRegistrationId: b, status: MatchStatus.READY })); const p1 = await direct(entries[0].registrationId, entries[1].registrationId, 1); if (entries.length === 3) { await direct(entries[0].registrationId, entries[2].registrationId, 2); await direct(entries[1].registrationId, entries[2].registrationId, 3); return this.matches(zoneId); } const p2 = await direct(entries[2].registrationId, entries[3].registrationId, 2); await this.m.save(this.m.create({ zoneId, matchOrder: 3, homeSource: ParticipantSource.WINNER, homeSourceMatchId: p1.id, awaySource: ParticipantSource.LOSER, awaySourceMatchId: p2.id, status: MatchStatus.PENDING })); await this.m.save(this.m.create({ zoneId, matchOrder: 4, homeSource: ParticipantSource.WINNER, homeSourceMatchId: p2.id, awaySource: ParticipantSource.LOSER, awaySourceMatchId: p1.id, status: MatchStatus.PENDING })); return this.matches(zoneId); } async result(id: number, homeScore: number, awayScore: number) { if (homeScore === awayScore) throw new BadRequestException('El partido debe tener ganador'); const match = await this.m.findOneBy({ id }); if (!match?.homeRegistrationId || !match.awayRegistrationId) throw new BadRequestException('El partido no esta listo'); match.homeScore = homeScore; match.awayScore = awayScore; match.winnerRegistrationId = homeScore > awayScore ? match.homeRegistrationId : match.awayRegistrationId; match.status = MatchStatus.PLAYED; await this.m.save(match); await this.resolveDependents(match); return match; } matches(zoneId: number) { return this.m.find({ where: { zoneId }, relations: { homeRegistration: true, awayRegistration: true }, order: { matchOrder: 'ASC' } }); } private async resolveDependents(source: TournamentMatch) { const matches = await this.m.find({ where: [{ homeSourceMatchId: source.id }, { awaySourceMatchId: source.id }] }); for (const match of matches) { const fill = async (side: 'home' | 'away') => { const sourceId = side === 'home' ? match.homeSourceMatchId : match.awaySourceMatchId; if (sourceId !== source.id) return; const sourceType = side === 'home' ? match.homeSource : match.awaySource; const loser = source.homeRegistrationId === source.winnerRegistrationId ? source.awayRegistrationId : source.homeRegistrationId; if (side === 'home') match.homeRegistrationId = sourceType === ParticipantSource.WINNER ? source.winnerRegistrationId : loser; else match.awayRegistrationId = sourceType === ParticipantSource.WINNER ? source.winnerRegistrationId : loser; }; await fill('home'); await fill('away'); if (match.homeRegistrationId && match.awayRegistrationId) match.status = MatchStatus.READY; await this.m.save(match); } } }
