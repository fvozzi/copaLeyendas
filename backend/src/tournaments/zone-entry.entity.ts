import { Column, Entity, JoinColumn, ManyToOne, PrimaryGeneratedColumn, Unique } from 'typeorm'; import { PairRegistration } from '../registrations/pair-registration.entity'; import { Zone } from './zone.entity';
@Entity('zone_entries') @Unique(['zoneId', 'registrationId']) export class ZoneEntry { @PrimaryGeneratedColumn() id: number; @Column() zoneId: number; @ManyToOne(() => Zone, { onDelete: 'CASCADE' }) @JoinColumn({ name: 'zoneId' }) zone: Zone; @Column() registrationId: number; @ManyToOne(() => PairRegistration, { onDelete: 'RESTRICT' }) @JoinColumn({ name: 'registrationId' }) registration: PairRegistration; @Column({ default: 0 }) seed: number; }
