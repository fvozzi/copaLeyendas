import { Column, Entity, JoinColumn, ManyToOne, PrimaryGeneratedColumn } from 'typeorm'; import { Category } from '../categories/category.entity'; import { Tournament } from './tournament.entity';
@Entity('tournament_categories') export class TournamentCategory { @PrimaryGeneratedColumn() id: number; @Column() tournamentId: number; @ManyToOne(() => Tournament, { onDelete: 'CASCADE' }) @JoinColumn({ name: 'tournamentId' }) tournament: Tournament; @Column() categoryId: number; @ManyToOne(() => Category, { onDelete: 'RESTRICT' }) @JoinColumn({ name: 'categoryId' }) category: Category; @Column({ default: 25 }) pointsPerSet: number; @Column({ default: 1 }) setsToWin: number; @Column({ default: 4 }) zoneSize: number; @Column({ default: true }) registrationsOpen: boolean; }
