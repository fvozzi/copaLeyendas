import { Column, CreateDateColumn, Entity, PrimaryGeneratedColumn, UpdateDateColumn } from 'typeorm'; import { TournamentStatus } from './tournament.enums';
@Entity('tournaments') export class Tournament { @PrimaryGeneratedColumn() id: number; @Column() name: string; @Column({ type: 'date', nullable: true }) startsAt: string | null; @Column({ type: 'date', nullable: true }) endsAt: string | null; @Column({ type: 'varchar', nullable: true }) city: string | null; @Column({ type: 'enum', enum: TournamentStatus, enumName: 'tournament_status', default: TournamentStatus.DRAFT }) status: TournamentStatus; @CreateDateColumn() createdAt: Date; @UpdateDateColumn() updatedAt: Date; }
