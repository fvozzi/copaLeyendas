name: Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: deploy-production
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      NODE_VERSION: 20
      RELEASE_NAME: copa-leyendas-release.tgz
      VITE_API_URL: ${{ vars.VITE_API_URL || '/api' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: |
            backend/package-lock.json
            frontend/package-lock.json

      - name: Install backend dependencies
        run: npm --prefix backend ci

      - name: Lint backend
        run: npm --prefix backend run lint

      - name: Test backend
        run: npm --prefix backend run test

      - name: Build backend
        run: npm --prefix backend run build

      - name: Prune backend dev dependencies
        run: npm --prefix backend prune --omit=dev

      - name: Install frontend dependencies
        run: npm --prefix frontend ci

      - name: Lint frontend
        run: npm --prefix frontend run lint

      - name: Test frontend
        run: npm --prefix frontend run test

      - name: Build frontend
        run: npm --prefix frontend run build
        env:
          VITE_API_URL: ${{ env.VITE_API_URL }}

      - name: Create release archive
        run: |
          mkdir -p release/backend release/frontend release/deploy
          cp backend/package.json backend/package-lock.json release/backend/
          cp -R backend/dist release/backend/dist
          cp -R backend/node_modules release/backend/node_modules
          cp -R frontend/dist release/frontend/dist
          cp -R deploy/. release/deploy/
          tar -czf "${RELEASE_NAME}" -C release .

      - name: Prepare deploy SSH key
        env:
          DROPLET_SSH_KEY: ${{ secrets.DROPLET_SSH_KEY }}
        run: |
          install -d -m 700 ~/.ssh
          printf '%s' "${DROPLET_SSH_KEY}" | tr -d '\r' > ~/.ssh/deploy_key.raw

          if grep -qE 'BEGIN (OPENSSH|RSA|EC|DSA|PRIVATE) PRIVATE KEY' ~/.ssh/deploy_key.raw; then
            mv ~/.ssh/deploy_key.raw ~/.ssh/deploy_key
          else
            base64 -d ~/.ssh/deploy_key.raw > ~/.ssh/deploy_key
            rm -f ~/.ssh/deploy_key.raw
          fi

          chmod 600 ~/.ssh/deploy_key

      - name: Add droplet host key
        run: ssh-keyscan -p "${{ secrets.DROPLET_PORT }}" -H "${{ secrets.DROPLET_HOST }}" >> ~/.ssh/known_hosts

      - name: Upload release archive
        run: scp -i ~/.ssh/deploy_key -o IdentitiesOnly=yes -P "${{ secrets.DROPLET_PORT }}" "${RELEASE_NAME}" "${{ secrets.DROPLET_USER }}@${{ secrets.DROPLET_HOST }}:/tmp/${RELEASE_NAME}"

      - name: Deploy on droplet
        run: |
          ssh -i ~/.ssh/deploy_key -o IdentitiesOnly=yes -p "${{ secrets.DROPLET_PORT }}" "${{ secrets.DROPLET_USER }}@${{ secrets.DROPLET_HOST }}" \
            "sudo RELEASE_ARCHIVE=/tmp/${RELEASE_NAME} bash /var/www/copa-leyendas/app/deploy/server/deploy.sh"
